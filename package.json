{
  "name": "cloudflare-routing-test",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy:origins": "pnpm --filter origin-one run deploy && pnpm --filter origin-two run deploy",
    "deploy:router": "pnpm --filter cf-worker-router run deploy",
    "deploy:all": "pnpm run deploy:origins && pnpm run deploy:router",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "lefthook": "^2.1.4",
    "oxfmt": "^0.41.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.75.0"
  },
  "packageManager": "pnpm@10.32.1"
}
